#-*- coding: utf-8 -*-

from distutils.core import setup


packages = (
    'archive_clean',
    )

setup(name='archive_clean', 
      version='0.0.2.1', 
      packages=packages, 
      description='Archive file cleaner.',
      author='Small', 
      author_email='small@bluemask.net')

